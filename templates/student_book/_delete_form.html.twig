<form method="post" action="{{ path('app_student_book_delete', {'id': student_book.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ student_book.id) }}">
    <button class="btn">Supprimer</button>
</form>
