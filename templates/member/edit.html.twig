{% extends 'article/base.html.twig' %}

{% block title %}Modifier le membre{% endblock %}

{% block body %}
  <div class="registration-form">
    <div class="container">
      {{ include('member/_form.html.twig', { title: "Mettre a jour le membre"}, { button_label: 'Mettre a jour'
          }
        )
      }}

      {# {{ include('member/_delete_form.html.twig') }} #}
      <a href="{{ path('app_member_index') }}">
        Retour à la liste des membres
      </a>
    </div>
  </div>

    {# {{ include('member/_form.html.twig', {'button_label': 'Mettre a jour'}) }} #}

    {# <a href="{{ path('app_member_index') }}">Retour a la liste</a> #}

    {# {{ include('member/_delete_form.html.twig') }} #}
{% endblock %}
